<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nestana.infamily">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".utils.ApplicationClass"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.categories.CategoriesActivity"
            android:label="@string/categories" />
        <activity
            android:name=".ui.articles.ArticlesActivity"
            android:label="@string/articles" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".ui.articles_detailed.ArticleDetailedActivity"
            android:label="@string/article" />
        <activity
            android:name=".ui.check_document_list.CheckListActivity"
            android:label="@string/check_list" />
        <activity android:name=".ui.FutureParentsActivity"/>
    </application>

</manifest>